A solution for the palindrome detector kata using PHP.

Target Environment: `php-7.x`

Setup solution:

```
composer install
```

Run tests:

```
composer run test
```

Lint code:

```
composer run lint
```
